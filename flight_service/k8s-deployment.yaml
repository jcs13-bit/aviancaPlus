apiVersion: apps/v1
kind: Deployment
metadata:
  name: flights-service-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flights-service-app
  template:
    metadata:
      labels:
        app: flights-service-app
    spec:
      containers:
        - name: flights-service-app
          image: docker.io/jose0613/flights_service:v1.0.2
          ports:
            - containerPort: 5000
          env:
            - name: DB_HOST
              value: "postgres-service"
            - name: DB_NAME
              value: "flights"
            - name: DB_USER
              value: "postgresuser"
            - name: DB_PASSWORD
              value: "password"
            - name: DB_PORT
              value: "5432"

---
apiVersion: v1
kind: Service
metadata:
  name: flights-service
spec:
  type: NodePort
  selector:
    app: flights-service-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30002
